import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks'

import UserImporter from '.'

<Meta title="Components/UserImporter" component={UserImporter} />

export const Template = args => <UserImporter {...args} />
export const defaultProps = {}

# UserImporter

This is just a full feature component that uses the `UserItem`, `UserList`,
`DropCVSJSONFile` components. Using this component you'll be able to upload
`CSV` and `JSON` files and preview this data on your browser. It can also be
used to fetch and show data on an external endpoint.

## Props

| Name  | Description                      | Type         | default |
| ----- | -------------------------------- | ------------ | ------- |
| style | CSS styles applied to components | `CSS Object` | `{}`    |

## CSS

```
{
    drop: { /*CSS JS*/},
    list: { /*CSS JS*/},
    title: { /*CSS JS*/},
    h2: { /*CSS JS*/},
    h3: { /*CSS JS*/},
    button: { /*CSS JS*/},
    loadMore: { /*CSS JS*/}
}
```

<Canvas>
    <Story name="main" args={defaultProps}>
        {Template.bind({})}
    </Story>
</Canvas>
