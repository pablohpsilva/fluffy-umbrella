import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks'

import DropCSVJsonFile from '.'

<Meta title="Components/DropCSVJsonFile" component={DropCSVJsonFile} />

export const Template = args => <DropCSVJsonFile {...args} />
export const defaultProps = {
    text: 'Please click (or drop) here your .CSV or .JSON file',
    accept: '.json,.csv',
    multiple: false,
    size: 1e7,
    onChange: ev => console.log('changed', ev),
    onError: error => null
}

<Canvas>
    <Story name="main" args={defaultProps}>
        {Template.bind({})}
    </Story>
</Canvas>
